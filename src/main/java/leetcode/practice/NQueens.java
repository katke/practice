package leetcode.practice;

import shared.PracticeStatus;

public class NQueens {
  String source = "https://leetcode.com/explore/learn/card/recursion-ii/472/backtracking/2804/";
  PracticeStatus practiceStatus = PracticeStatus.IN_PROGRESS;
  String timeComplexity = "";
  String spaceComplexity = "";
  /*
The n-queens puzzle is the problem of placing n queens on an n x n chessboard such that no two queens attack
each other.

A queen can attack any piece that is situated at the same row, column or diagonal of the queue.

Given an integer n, return the number of distinct solutions to the n-queens puzzle.

1 <= n <= 9
*/

  int solution(int n) {
    if (n == 0) return 0;

    return 0;
  }
}
